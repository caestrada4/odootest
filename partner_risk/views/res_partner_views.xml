<odoo>
  <record id="view_partner_form_inherit_risk" model="ir.ui.view">
    <field name="name">res.partner.form.inherit.risk</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="base.view_partner_form"/>
    <field name="arch" type="xml">
      <!-- insertar después del campo 'name' para máxima compatibilidad -->
      <xpath expr="//field[@name='category_id']" position="after">
          <field name="tipo_deudor"/>
          <field name="es_residente"/>
      </xpath>
    </field>
  </record>
  
</odoo>
